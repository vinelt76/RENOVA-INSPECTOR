# ── App: React + Vite + Capacitor (app/) ──────────────────────────────
app/node_modules/
app/dist/
app/.vite/
app/coverage/
app/.env
app/.env.local

# Capacitor / iOS (proyecto nativo generado)
app/ios/
app/.capacitor/
# app/android/ se commitea al repo para que CI/CD pueda hacer cap sync + gradlew
*.apk
*.aab
*.keystore

# ── Referencia Python (reference/) ────────────────────────────────────
reference/__pycache__/
reference/**/__pycache__/
reference/**/*.pyc

# ── Docs / Data (Excels reales — local only) ──────────────────────────
docs/archive/
*.xlsx
*.xls
*.docx

# ── Sistema ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.zip
node_modules/

# ── Supabase (dashboards HTML demo) — credenciales locales, jamás al repo ──
supabase-config.local.js
