# T11 — Smoke test de dashboards en navegador

**Método:** servidor estático local (`python3 -m http.server 8934` sobre `WEB/`, detenido al
cerrar el task) + Chrome MCP, solo lectura, sin credenciales reales.

## Confirmado — las 4 páginas cargan limpio y exigen sesión antes de tocar datos

`servicios.html`, `inventario.html`, `rendimiento.html`, `Inspecciones por unidad.html`: las
cuatro renderizan el modal `RENOVA INSPECTOR — Iniciar sesión` de `requireAuth()`
(`WEB/supabase-demo.js`) antes de mostrar cualquier dato, con **cero mensajes en consola** (ni
error, ni warning, ni log) durante la carga. Ninguna hizo fetch a Supabase ni expuso datos antes
de autenticar — correcto por diseño.

## Corrección sobre el plan original

`WEB/buscador/` no tiene un HTML de entrada propio (`index.html` da 404) — es un módulo
(`buscador.css`, `data.js`, `finder-controller.js`, `search-model.js` + su propio
`package.json`/`vitest.config.js`/`__tests__/`) que se monta dentro de otra pantalla, no una
página independiente navegable. Se ajusta la lista: se verificaron las 4 páginas HTML de nivel
superior encontradas (`servicios`, `inventario`, `rendimiento`, `Inspecciones por unidad`), no
"buscador" como quinta URL.

## No verificado — requiere credenciales reales, fuera del alcance de "solo lectura sin cuentas"

El smoke test completo que pide `CLAUDE.md` ("consola limpia, datos visibles, recarga
persistente") **no se pudo completar** más allá del gate de login: no se dispone de credenciales
de prueba, y crear una cuenta o autenticar está fuera del alcance acordado para este recorrido
(no se piden credenciales al usuario a mitad de una verificación de solo lectura). Queda
pendiente como paso manual: con una cuenta real de alguna empresa demo, repetir esta prueba y
confirmar que los datos mostrados son consistentes con las cifras de T10 (288 inspecciones, 2247
mediciones) y que `pressure_state_fixed` (T08) se ve tal cual se documentó.

## Severidad consolidada de este task

Sin hallazgos nuevos — el login gate en sí funciona limpio en las 4 páginas. La verificación de
datos post-login queda como trabajo manual pendiente, declarado explícitamente (no silenciado).
