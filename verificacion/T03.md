# T03 — Cobertura de casos límite

**Método:** revisar que `golden.json` cubra los casos límite listados en el plan antes de darlo
por completo. Ejecutado como parte de T01/T02 (mismo archivo, mismo comparador).

| Caso límite pedido | Cubierto en `golden.json` | Resultado |
|---|---|---|
| Canal D opcional en cualquier eje | `rtdmovi-d-en-direccion`, `idi-3-canales-ignora-d` | OK, paridad y spec |
| RTD negativo | `rtdmovi-negativo-a`, `rtdmovi-negativo-d` | OK — ambas implementaciones lanzan error |
| `sinMedir` | `presion-sin-medir-null`, `presion-sin-medir-flag` | OK — flag gana sobre valor numérico presente |
| `km_actual == km_anterior` | `tasa-km-iguales` | OK — `null`, no división por cero |
| Tasa negativa | `vur-tasa-negativa`, `tasa-rtd-aumenta` | OK — VUR null "dato inválido" |
| RTD > 22 mm | `rtdmovi-sobre-22mm` | OK — no rechaza (correcto: spec pide advertencia de UI, no bloqueo de cálculo) |
| Presión = 0 (medición real, no ausencia) | `presion-cero` | OK — clasifica Baja Presión, no Sin Medir |
| Cambio inmediato con y sin tasa | `vur-cambio-inmediato*`, `vur-cambio-inmediato-sin-tasa` | OK — el segundo caso queda documentado como ambiguo (ver T02 hallazgo 2) |
| Umbrales no-default (empresa configurada distinto) | `estadortd-umbrales-personalizados` | OK — confirma que la función no hardcodea 4/7 |

48 casos totales, 0 sin cubrir de la lista pedida. Los 5 marcados `spec_ambigua` están
documentados en T02 y no bloquean el veredicto de paridad (que es 48/48 exacto).
