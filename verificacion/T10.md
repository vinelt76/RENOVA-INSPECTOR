# T10 — Coherencia cruzada de datos en producción

**Método:** consultas de solo lectura contra `fbxupwwgiebhlciqftpw`, contrastando contra las
cifras que `knowledge/ai/10 - Roadmap deuda y riesgos.md` documenta al 2026-07-23.

| Métrica | Documentado (2026-07-23) | Medido ahora (2026-07-24) | Estado |
|---|---|---|---|
| `inspections` | — | 288 | — |
| `inspection_measurements` | — | 2247 | — |
| `v_inspection_dashboard_rows` (filas) | — | 2247 | 1:1 con measurements, correcto |
| Mediciones sin `life_cycle_id` | 2183/2247 | 2183/2247 | **Vigente, exacto** |
| Posiciones `baseline_pending` | ~2094 | 2094 | **Vigente, exacto** |
| Mediciones de unidad `QA-CN16` (dato de prueba) | 14 | 14 | **Vigente, exacto** — sigue en producción, no filtrado |
| Códigos de neumático distintos en `QA-CN16` | 9 cascos (nota) | 7 códigos distintos en mediciones | Cercano, no idéntico — ver nota abajo |
| `tire_casings` sin código | ~316 | **3** de 40 totales | **Discrepancia grande — ver hallazgo** |
| Variantes de caja en `brand_name` | 13 valores / 2247 filas (nota original) | 17 valores crudos → 14 al normalizar `upper(trim())` | Consistente con la nota (colapsa ~3 variantes de caja) |
| `v_tire_services` (filas) | límite sin paginación de 2000 | **5** | Lejos del límite — riesgo no materializado hoy |

## Hallazgo — "~316 cascos sin código" no reproducible: la tabla real solo tiene 40 filas — INFORMATIVO, requiere aclaración humana

`tire_casings` tiene **40 filas totales en producción**, de las cuales solo 3 no tienen `code`.
La cifra de deuda (~316) es un orden de magnitud mayor que la tabla completa hoy. Dos lecturas
posibles, ninguna confirmable por lectura de datos sola:

1. La cifra documentada es de una fecha/snapshot distinto y `tire_casings` fue reconstruida o
   vaciada parcialmente desde entonces (posible, dado que `reconciliation_status` está descrito
   como "100% pending sin reconciliador" — el proceso que llenaría `tire_casings` desde
   `inspection_measurements` podría no haber corrido aún, o haber corrido parcialmente y luego
   revertido).
2. La cifra de deuda contaba otra cosa (p. ej. "cascos alcanzables por código sin historial"
   contra un universo distinto a la tabla `tire_casings` actual — quizás un conteo derivado de
   `inspection_measurements.tire_code` sin JOIN a `tire_casings`).

Siguiendo la regla de `CLAUDE.md` ("un conflicto entre intención e implementación no se resuelve
silenciosamente"), esto se deja como discrepancia declarada — no se ajusta la nota de deuda ni
se asume cuál de las dos lecturas es correcta sin que una persona del equipo lo confirme.

## Confirmado — datos `QA-TEST` siguen en producción sin resolución

La unidad `QA-CN16` (placa) sigue en la tabla `units` con 14 mediciones asociadas, exactamente
como registra la deuda. No se filtró, no se limpió. `tire_casings` no tiene ninguna fila con
código que contenga literal `QA` o `QA-TEST` — los "9 cascos" mencionados en la deuda
probablemente refieren a códigos de neumático dentro de `inspection_measurements.tire_code`
(7 distintos medidos ahí) más algún casing reconciliado que no matchea el patrón de búsqueda
usado aquí; no se investiga más a fondo por estar fuera del alcance de "solo lectura ligera" de
este task.

## No verificado en este recorrido

- `reconciliation_status` — no se consultó su distribución real (columna existe según el
  esquema documentado, pero no se ejecutó el `GROUP BY` para confirmar "100% pending").
- `tire_movement_executions` fuera de la publicación `supabase_realtime` — no verificado
  (requeriría consultar `pg_publication_tables`, fuera del set de queries priorizadas hoy).

## Severidad consolidada de este task

| # | Hallazgo | Severidad |
|---|---|---|
| 1 | "~316 cascos sin código" no reproducible contra el estado actual (`tire_casings` tiene 40 filas, 3 sin código) | **Informativo / requiere aclaración humana** — no es un defecto de la app, es una nota de conocimiento potencialmente desactualizada o mal especificada |
| 2 | QA-TEST sigue en producción | Ya conocido, confirmado vigente |
