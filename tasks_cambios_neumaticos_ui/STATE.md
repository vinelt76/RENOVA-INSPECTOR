# STATE — Fase 2 UI · Modo Cambios de Neumáticos

Estados: PENDIENTE / EN CURSO / EN REVISIÓN / APROBADO ✓ / EN CORRECCIÓN ⚠ /
**BLOQUEADA POR DECISIÓN HUMANA**.

Cada ejecutor actualiza SU fila al iniciar y al terminar (estado, resultado con evidencia,
revisión). No editar filas ajenas salvo la columna "Revisión" al revisar. Ninguna tarea depende de
la conversación que originó este plan: toda la información vive en estos archivos y en
`tasks_cambios_neumaticos/CONTRATOS_UI.md`.

Regla de exclusión: nunca ejecutar en paralelo dos tareas que escriban el mismo archivo. Las
tareas que tocan `WEB/tire-change/cambios-controller.js` o el HTML están encadenadas por
dependencias (09→10→11→13→14).

| Tarea | Título | Propietario | Estado | Depende de | Archivos exclusivos | Resultado esperado | Revisión |
|---|---|---|---|---|---|---|---|
| task_01 | Cierre de decisiones humanas bloqueantes | CLAUDE | APROBADO ✓ | — | `DECISIONES.md` (actualización) | **Las 10 decisiones RESUELTAS por el humano (2026-07-13).** D1=tabs sobre el diagrama · D2=selección por toques · D3=bucket privado+URL firmada · D5=banner sin recargar · D6=odómetro obligatorio · D8=pin exacto+retry · D10=unidad de prueba dedicada+limpieza. Propagado a task_09/task_13/PLAN. Desbloqueadas task_03/task_12/task_16. Pendiente insumo humano: placa/credenciales de prueba (vault) y versión exacta de supabase-js | Auto-registrado; sin código de producción tocado |
| task_02 | Andamiaje de módulos + runner de pruebas (vitest scoped) | CLAUDE | APROBADO ✓ | — | `WEB/tire-change/{package.json,vitest.config.js,README.md,package-lock.json}`, `WEB/tire-change/__tests__/smoke.test.js` | Estructura `WEB/tire-change/` creada. Runner **vitest 4.1.9** (exacto, = app/), **entorno `node`** (los módulos con DOM se validan por smoke, no unit → sin happy-dom/jsdom). `npm install` + `npm test` → **1/1 verde** (Node v22.22.2, npm 10.9.7). `node_modules` ignorado por `.gitignore:32`; se rastrean 5 archivos fuente + lockfile. README documenta límites de módulos y convenciones de mock/inyección | Auto-verificado: `npm test` verde; sin lógica de negocio |
| task_03 | Storage: bucket privado + RLS de `storage.objects` | CLAUDE | APROBADO ✓ | task_01 | `supabase/migrations/20260715000000_tire_discard_photos_bucket.sql`, `supabase/tests/tire_discard_photos.test.sql` | **Migración aplicada al remoto (aprobación humana) → `{success:true}`.** Bucket privado `tire-discard-photos` (public=false, 5242880, [jpeg,webp]) + 3 policies `authenticated` (INSERT/SELECT por empresa vía `(storage.foldername(name))[1]=current_company_id()`; DELETE por owner+empresa); sin `anon`; idempotente. **Test remoto → `P0001: TESTS_PASSED`** (T0 privado, T1 insert propio, T2 insert cruzado rechazado 42501, T3/T4 aislamiento SELECT, T7 anon sin acceso, 3 policies presentes); residuo=0. **Hallazgo**: `storage.protect_delete()` bloquea DELETE por SQL directo → el borrado de huérfanos (task_12) usa Storage API `.remove()`, gobernado por la policy DELETE (nota agregada a task_12) | `sync-migration-reviewer`: **riesgo BAJO, sin P0/P1**; 3 P2 (2 abordados: nota owner_id + test anon; 1 aceptado por Decisión 3) |
| task_04 | Capa de datos: resolución de unidad + carga de vistas + normalización | CODEX | APROBADO ✓ | task_02 | `WEB/tire-change/data.js`, `WEB/tire-change/__tests__/data.test.js` | `resolveUnitId` prioriza placa y usa fallback por inspección; loaders con selects exactos (28/15 columnas), orden canónico, `numeric`→Number preservando `NULL`, 0 filas sin posiciones inventadas y errores propagados. Inyección: segundo argumento acepta `fetchView` o cliente `{ fetchView }`; sin argumento usa `globalThis.RenovaSupabase`. Casos: placa/inspection_id, no visible, 6/8 posiciones, vacía, inventario con NULL, orden, red y sin `company_id` en filtros | Auto-verificado: `cd WEB/tire-change && npm test` → **2 archivos, 11/11 pruebas verdes**; sin whitespace residual |
| task_05 | Modelo puro del lote: máquina de estados + invariantes + payload v1 | CLAUDE | PENDIENTE | task_02 | `WEB/tire-change/batch-model.js`, `WEB/tire-change/__tests__/batch-model.test.js` | Alta/baja/edición de movimientos, invariantes, sellado inmutable, payload v1 exacto; pruebas de cada invariante | — |
| task_06 | Persistencia local: borrador + payload sellado idempotente | CODEX | APROBADO ✓ | task_02, task_05 | `WEB/tire-change/batch-store.js`, `WEB/tire-change/__tests__/batch-store.test.js` | APIs `save/load/clearDraft` y `save/load/clearSealed`; claves `renova:tire-change:draft:<user>:<company|->:<unit>` y `renova:tire-change:sealed:<user>:<company|->:<unit>:<batch_id>`, con sobre versionado que revalida sesión/unidad. Borrador como snapshot editable compatible con `BatchModel.state`; sellado profundamente congelado, primer contenido inmutable y conflicto si cambia `batch_id`. Fallback/tombstone en memoria ante cuota, storage bloqueado o `removeItem` fallido. Casos: recarga editable, sellado pendiente, aislamiento usuario/empresa/unidad, mismatch limpiado, retry idéntico, conflicto, limpieza y degradación | Auto-verificado contra `createBatchModel`/`seal`: `npm test -- batch-store` → **13/13**; suite `npm test` → **6 archivos, 90/90**; `node --check` y whitespace verdes |
| task_07 | Cliente RPC + clasificación de errores + pin supabase-js | CLAUDE | PENDIENTE | task_02, task_05 | `WEB/tire-change/rpc.js`, `WEB/tire-change/__tests__/rpc.test.js`, edición de `WEB/supabase-demo.js` (pin) | Llamada única, `classifyBatchError` de las 5+ clases, retry inmutable; versión fijada | — |
| task_08 | Proyección pura del diagrama (ocupada/vacía/origen/destino/…/conflicto) | CODEX | APROBADO ✓ | task_02, task_05 | `WEB/tire-change/diagram-projection.js`, `WEB/tire-change/__tests__/diagram-projection.test.js` | `project(remoteState, draft, selected)` devuelve `Map` ordenado sin inventar posiciones. Diccionario: ocupación `occupied/empty`; roles `none/origin/destination/swapA/swapB`; flags `mismatch/conflict/selected/retention/discard/mount/swap`; labels `OCUPADA`, `VACÍA`, `A RETÉN`, `DESCARTE`, `MONTAR`, `SWAP A/B`, `REVISAR IDENTIDAD`, `CONFLICTO`. Proyecta retiros antes de destinos, acepta `conflictPositions` de validación/RPC y no muta entradas. Casos: 6/8, vacía seleccionada, retén, descarte, mount vacío/libre-tras-retiro, swap, mismatch no bloqueante, doble uso/ciclo duplicado, stale/RPC, pureza/determinismo | Auto-verificado: `npm test -- diagram-projection` → **12/12**; suite scope `npm test` → **3 archivos, 23/23**; `node --check` verde; sin DOM/CSS/HTML |
| task_09 | Selector de modo + controlador + integración HTML mínima | CODEX | APROBADO ✓ | task_04, task_08 | `WEB/tire-change/{mode-toggle.js,cambios-controller.js,diagram-view.js,tire-change.css}`, edición de `WEB/Inspecciones por unidad.html` | Tabs accesibles **Inspección/Cambios** sobre el gemelo; `?mode=cambios` persistente sin recarga; panel/dock y selección aislados de `POSICIONES`; carga perezosa `resolveUnitId` → vistas en paralelo tras reutilizar la sesión existente; estados idle/loading/empty/error/ready; estado vivo y hooks `getState/subscribe/selectPosition/setDraft/reload/setMode`; dock semántico y ruedas P1–P8 desde `project`, incluidas vacías y `REVISAR IDENTIDAD`. Smoke local: HTML/JS/CSS HTTP 200; URL sin `mode` → Inspección visible, con `mode=cambios` → Cambios visible y un solo login; fixture CDP de 6 posiciones → 6 botones/6 ruedas, 1 vacía visible y seleccionable; vuelta a Inspección restauró panel+dock y quitó `mode`; sin credenciales/datos reales. | Auto-verificado: `node --check` en 3 módulos; `npm test` → **6 archivos, 91/91**; `npm run docs:check` verde. Smoke autenticado con unidad real queda en `task_16` (requiere credenciales de prueba). |
| task_10 | Flujos de movimiento: retén, descarte, montaje, swap | CODEX | APROBADO ✓ | task_09, task_05 | `WEB/tire-change/movements-ui.js` (+ sección namespaced en `cambios-controller.js`) | Tarjeta contextual: ocupada→retén/descarte/swap, vacía→solicitud de montaje. Altas pasan por `BatchModel`, conservan `expected_life_cycle_id` y refrescan `project`; swap por selección A→B desde dock/rueda con cancelación. Reutiliza modal de descarte sin duplicar handlers históricos: enum exacto + evidencia `pending://task-12/...`, confirmación deshabilitada hasta completar ambos. Montaje emite `renova:tire-change:request-mount` y se completa mediante `RenovaTireChange.movements.addMount(...)` para task_11. Inválidos muestran violaciones escapadas y no alteran borrador | Smoke Playwright local con fixture 6 posiciones: retén P1 + descarte P2 + mount P3 + swap P4↔P5, 4 ops/ciclos/causa/proyección exactos; duplicado bloqueado; dock operado por teclado; consola limpia; sin red/escrituras Supabase. `npm test` → **6 archivos, 91/91**; `node --check` y whitespace verdes |
| task_11 | Inventario/buscador + prevención de duplicados | CODEX | APROBADO ✓ | task_10, task_04 | `WEB/tire-change/inventory-ui.js` (+ sección en `cambios-controller.js`) | Cajón accesible sobre `state.inventory`, abierto por `renova:tire-change:request-mount`; buscador tokenizado sin acentos por código/marca/modelo/medida/condición/diseño. Renderiza identidad, catálogo y días; `NULL` finales se muestran como `SIN RETIRO PREVIO`. `mountedLifeCycleIds` deriva ciclos usados desde el borrador en cada render: permanecen visibles pero deshabilitados y se revalidan al click; el alta invoca `task10MovementsUi.addMount`, manteniendo `BatchModel` como segunda barrera. Estados vacío/sin resultados/error y cierre Escape cubiertos; API `RenovaTireChange.inventory.open/close` expuesta | Helpers `npm test -- inventory` → **5/5**; suite → **7 archivos, 96/96**. Smoke Playwright local: filtro `michelin 295 r1`, mount P3, mismo ciclo deshabilitado/bloqueado en P6, segundo ciclo con NULL montado por teclado, proyección correcta y consola limpia; sin red/escrituras Supabase. `node --check` y whitespace verdes |
| task_12 | Foto real de descarte a Storage (captura/preview/upload/limpieza) | CLAUDE | PENDIENTE (D3 resuelta) | task_03, task_10 | `WEB/tire-change/storage-client.js` (+ integración en `movements-ui.js` descarte) | Upload pre-sellado con `photo_url` real, preview, borrado de huérfanos, reintento | — |
| task_13 | Resumen/deshacer/editar + confirmación + errores + retry + Realtime | CLAUDE | APROBADO ✓ | task_11, task_06, task_07, task_12 | `WEB/tire-change/summary-confirm.js`, `WEB/tire-change/__tests__/summary-confirm.test.js` (+ sección namespaced en `cambios-controller.js`) | Resumen ordenado por `seq` con editar RTD/causa/notas y deshacer (limpia foto de descarte); encabezado persistente con fecha local editable, odómetro entero obligatorio y notas; advertencia no bloqueante contra `installed_at`. Scope local por usuario/empresa/unidad restaura borrador y sellado. Confirmación `seal→saveSealed→applyPendingBatch`; éxito limpia y recarga; red conserva/reintenta payload idéntico; dominio vuelve a edición; `stale_state` descarta/recarga; mensajes cubren `invalid_batch`, `unavailable_cycle`, `occupied_position`, `forbidden`, fecha y desconocido. Realtime: con borrador/sellado muestra banner, sin borrador recarga | `npm test` → **8 archivos, 101/101**. Smoke Playwright local: recarga restauró movimiento+encabezado; editar/deshacer disponible; Realtime con borrador no recargó y sin borrador sí; éxito hizo **1 RPC**, payload exacto y storage limpio; fallo de red conservó draft+sealed, 1 retry automático acotado y retry manual con payload idéntico; consola limpia salvo 2 logs RPC esperados del fallo simulado |
| task_14 | Accesibilidad y responsive | CODEX | APROBADO ✓ | task_13 | `WEB/tire-change/a11y.js`, `WEB/tire-change/__tests__/a11y.test.js`, ediciones a `diagram-view.js`, `inventory-ui.js`, `movements-ui.js`, `summary-confirm.js`, `tire-change.css` | Helper común de trap dinámico con Tab/Shift+Tab, Escape y restauración; descarte e inventario con semántica de diálogo y foco atrapado; ruedas 3D operables por Enter/Espacio y restauradas al volver a Inspección; dock nativo con flechas/Home/End y foco preservado tras render; edición del resumen enfoca/restaura controles. Objetivos ≥44 px, foco visible único naranja, `prefers-reduced-motion`; a ≤900 px escenario/panel apilados y a 390 px escena cede al dock horizontal sin overflow del body | `npm test` → **10 archivos, 132/132**. Smoke Playwright local con fixture sin escrituras: flujo desktop solo teclado (dock, rueda, inventario, descarte, resumen), traps y Escape/restauración correctos; viewport 390×844 → body/panel 390 px, stage→panel, escena oculta, targets dock 56 px; consola 0 errores. HTML y lógica pura intactos |
| task_15 | Suite de pruebas automatizadas integrales | CODEX | APROBADO ✓ | task_04, task_05, task_06, task_07, task_08, task_13 | `WEB/tire-change/__tests__/integration.test.js` | Matriz integral sin navegador: datos/proyección (6, 8, vacía, 0 filas/no autorizada/sin posiciones); retén, descarte con causa+foto fixture, mount vacío/reemplazo, swap válido e inválidos/duplicados, mismatch; lote mixto de 4 tipos con payload v1 exacto y llamada RPC; inventario desaparecido, timeout con mismo objeto+`batch_id`, edición con UUID nuevo, recarga de borrador/sellado; clasificación y transición de stale/no disponible/ocupada/permiso/lote/fecha/desconocido | `npm test -- integration` → **1 archivo, 29/29**; suite completa `npm test` → **10 archivos, 132/132**; `node --check` y whitespace verdes. Sin llamadas remotas, datos reales ni secretos |
| task_16 | Smoke real E2E + documentación + revisión cruzada final | CLAUDE | APROBADO ✓ | task_14, task_15 | `tasks_cambios_neumaticos_ui/REVISION_FINAL.md`, `knowledge/ai/05`, `knowledge/ai/07` | **Veredicto APTO** (`REVISION_FINAL.md`, 2026-07-14). Smoke real Chrome/Playwright con sesión de taller (MÓVIL BUS) sobre unidad de prueba dedicada **QA-CN16** (2-4-2). Lote mixto de los 4 tipos —retén P1, descarte P2 con **foto real** a Storage, swap P3⇄P4, montaje P8 desde inventario— confirmado con **1 sola RPC** `confirm_tire_change_batch`; base verificada (batch applied=true, retiros+foto, P3⇄P4 cruzado, P8 montada). Persistencia tras recarga (estado + borrador editable restaurado). Error real: cambio concurrente en P5 → banner "el estado cambió" + bloqueo de retiro sobre vacía **sin perder borrador**. No-regresión de Inspección OK. **Consola sin errores de app** (único 404 = favicon de Chrome, ajeno); **sin secretos en logs**. `knowledge/ai/05` y `07` actualizados; `npm run docs:check` verde; suite 132/132. Unidad QA-CN16 conservada y reseteada a prístino para prueba de campo 2026-07-15; historia/foto del smoke limpiadas | Auto-verificado con evidencia de navegador real; 0 ⚠ bloqueantes (1 observación menor: tarjeta "UNIDAD EN BAHÍA" rotula "7 POS" contando ocupadas, no configuradas — panel de Inspección, no del modo Cambios) |

Hitos: **H0** = task_01+task_02+task_03 · **H1** = task_04..task_08 · **H2** = task_09+task_10+task_11 ·
**H3** = task_12+task_13 · **H4** = task_14+task_15+task_16.
