# RETOMA — 2026-07-01 (para Fable 5)

> Este doc resume dónde quedó el proyecto al cierre de la sesión anterior (Sonnet 5,
> 2026-06-28/29) para que la próxima sesión arranque sin reconstruir contexto desde cero.
> Fuente completa: `tasks_opencode/STATE.md` (histórico completo lote por lote).

## Estado del código (verificado por git, no solo por STATE.md)

- Último commit: `d404d05 "s"` (2026-06-29) — mezcla cambios de CI/CD Android + rediseño UI de
  la sesión "Fixes APK + Rediseño UI" (sección `SESIÓN 2026-06-28` en STATE.md).
- `app/android/` **ya existe y está commiteado** (a diferencia de lo que decía STATE.md como
  pendiente — el APK ya se generó/intentó vía CI, ver commits `313a2d7`, `069f8bb`).
- Working tree tiene cambios sin commitear: `CLAUDE.md` modificado (agregó sección "Design
  Context"), y sin trackear: `DESIGN.md`, `PRODUCT.md`, `IDEAS SUELTAS.txt`, `.impeccable/`,
  `.tokensave/`, `.bashrc`, `app/.repro.tmp.mjs`. Revisar con el usuario antes de tocar/borrar
  nada de esto — probablemente trabajo en curso (sistema de diseño con `/impeccable`).
- **`app/_archivo/GrillaBody.tsx`**: el componente `GrillaBody` (modo Grilla) fue movido a
  `_archivo/` en el último commit — parece que el modo Grilla quedó deprecado/pausado a favor
  de solo el modo Formulario. Confirmar con Facundo si es intencional antes de asumir que sigue
  vivo en algún flujo.

## Pendiente crítico heredado de la sesión anterior

**Smoke test en navegador NUNCA se corrió** sobre los cambios de la sesión "Fixes APK +
Rediseño UI" (auto-avance RTD, autocomplete MEDIDA, auto-selección de unidad, rediseño visual
FormBody, fuentes offline, safe-area-insets, cámara nativa). Solo se verificó `npm run build`
en verde. Según CLAUDE.md esto es **obligatorio antes de generar APK** — no se puede saltar.

Antes de cualquier otra cosa, correr:
```bash
cd app && npm run dev
```
y recorrer el flujo empresa → unidad → inspección, confirmando:
- Autocomplete con fondo navy funciona (Marca/Modelo/Válvula/Medida/Reencauche)
- Auto-avance RTD con teclado numérico (R1→R2→R3→R4)
- Auto-selección de unidad al escribir número exacto
- Campo MEDIDA muestra opciones del catálogo
- Cero errores en consola del navegador
- Si hay persistencia: capturar dato, recargar, confirmar que sobrevive

## Feedback de UX de Facundo — sin triage todavía

Dos fuentes con ideas de UX, **sin evaluar ni priorizar formalmente por Opus/Fable**:

1. `IDEAS SUELTAS.txt` — análisis largo de un tercero (probablemente otro LLM) sobre el flujo
   de inspección, + mensajes sueltos de Facundo (WhatsApp, 1/jul/2026) reaccionando a él. Ideas
   recurrentes de Facundo en los mensajes sueltos (más recientes, prioridad real):
   - Resaltar visualmente el campo activo al hacer click (borde naranja / iluminado, estilo Excel)
   - Bug: "se sigue rompiendo al buscar otra unidad o la que acabas de inspeccionar" — **posible
     bug funcional real, no solo UX**, investigar primero
   - Volver a ocultar datos del neumático por defecto (solo relevante si se cambia la llanta o
     es bus nuevo) — como acordeón que se expande al tocar el código de neumático
   - Selector de posiciones más centrado/ergonómico
   - Todo texto de subtítulos y datos precargados en blanco (contraste)
   - Scroll horizontal o vertical entre neumáticos (carrusel) en vez de selector aparte
   - Datos antiguos (marca/modelo/medida) se mantienen al cambiar solo el código

2. `tasks_opencode/mimoanalisi.md` — análisis de calidad de código (arquitectura/deuda técnica),
   361 líneas, generado 2026-06-27. Prioridad alta ahí: migraciones de SQLite no versionadas
   (`app/src/db/sqlite.ts` — `runMigrations()` es un bloque monolítico `CREATE TABLE IF NOT
   EXISTS` sin tabla `schema_version`, no soporta evolucionar el schema con datos ya guardados).
   Resto del doc no releído en detalle esta sesión — revisar completo antes de actuar.

3. `tasks_opencode/plan_lote3_estandarizacion.md` — plan de LOTE 3 (estandarización de datos +
   catálogos desde 5 Excels nuevos). Según STATE.md el data-prep y task_09 (refactor núcleo de
   datos) están **HECHO/APROBADO** (2026-06-27/28). Falta confirmar si quedó algo abierto de
   este lote — no revisado en detalle esta sesión.

> **ACTUALIZACIÓN (misma fecha, sesión posterior):** el triage ya se hizo — todo lo de abajo
> quedó convertido en task specs ejecutables: ver **LOTE 5** en `STATE.md`
> (task_12 → task_11 → task_13 → task_14). Este doc queda como contexto histórico.

## Siguiente paso recomendado (orden)

1. **Smoke test del navegador** (bloqueante, ver arriba) — confirmar que la sesión anterior no
   rompió nada antes de sumar más cambios.
2. Investigar el bug reportado por Facundo ("se rompe al buscar otra unidad") — puede ser
   funcional, no solo cosmético.
3. Recién con eso verde, triar las ideas de UX de `IDEAS SUELTAS.txt` con Facundo (cuáles se
   implementan, en qué orden) y las de `mimoanalisi.md` (deuda técnica, empezando por
   migraciones versionadas si se va a tocar el schema de nuevo).
4. Revisar los archivos sin commitear (`DESIGN.md`, `PRODUCT.md`, `.impeccable/`) — parecen ser
   la base para trabajar con `/impeccable`; confirmar con Facundo si están listos para commitear.
